# 大罗天 · 自进化系统认知核心

> 这是系统的"宪法"。任何 AI 助手在进入本项目时，必须先读取此文件。
> 它定义了系统的方向、边界、标准和思考顺序，不是命令清单，而是方向定义。

---

## 一、系统目标

把大罗天从一个"被动整理的笔记库"升级为"越用越稳的自进化知识系统"。

关键不是单次操作多完美，而是：
- **纠正经验能否沉淀为下次自动生效的规则**
- **重复错误能否被系统性消除**
- **系统结构能否随使用自我优化**

---

## 二、原则边界

### 必须做的事（红线）
1. **先读记忆再行动**：每次进入项目时，必须先读取 `Meta/memory/learned-rules.md`，了解已沉淀的约束
2. **扫描后必须处理，处理后必须写回**：不能只输出在聊天里
3. **纠正必须被记录**：用户每次纠正后，立即追加到 `Meta/memory/corrections.md`
4. **原文与提炼必须分离**：原始文字稿/原文在完成提炼后，必须移入 `90-归档/`
5. **Inbox 3天内必须处理**：不能囤积
6. **新提炼内容先放 Today/**：当天在 `00-行动中枢/Today/` 查看，次日归档到正确分类

### 禁止做的事（红线）
1. **不创建空文件、不填充内容**
2. **不大段无结构文字**（超过 3 行不分段、无列表）
3. **不移动或重命名已有顶层文件夹**（`skill`、`研究`、`示例项目` 等）
4. **不把新文件默认放到 `00-个人研究/`**（此目录已废除）
5. **不自动删除任何文件**（必须等用户确认）
6. **不在运营素材库放学习资料**（`90-归档/运营素材库/` 只放项目执行直接使用的文件）

---

## 三、完成标准

### 什么叫"做好了"

| 操作类型 | 完成标准 |
|---------|---------|
| 文件同步 | ① docx 已转 md ② md 已在正确分类目录 ③ 根目录无重复 ④ 已更新变更日志 |
| 分类整理 | ① 文件已按主题分散到子分类 ② 根目录无未分类文件 ③ 已添加 `[[ ]]` 双向链接 |
| 重复清理 | ① 已列出所有重复文件清单 ② 用户已确认 ③ 已删除并验证 ④ 已记录到 evolution-log |
| 项目总览更新 | ① 资产清单已更新 ② 进度判断表已更新 ③ 下一步建议已更新 ④ 最后更新时间已刷新 |
| 纠正处理 | ① 已修复问题 ② 已记录到 corrections.md ③ 已提炼为 learned-rules ④ 已验证规则生效 |

### 质量检查清单（每次操作后必查）

- [ ] 是否违反了 learned-rules.md 中的任何规则？
- [ ] 分析结果是否写回到了具体文件？（不能只输出在聊天里）
- [ ] 如果移动了文件，是否检查了 Wikilinks 断链？
- [ ] `00-行动中枢/Meta/日志/变更日志.md` 是否已更新？
- [ ] 是否需要新增/修正 learned-rules？

---

## 四、复杂任务思考顺序

遇到需要多步骤处理的复杂任务时，按以下顺序思考：

```
Step 1: 读记忆
  └─→ 读取 learned-rules.md（了解已沉淀的约束）
  └─→ 读取 corrections.md 最近 3 条（了解最近纠正了什么）

Step 2: 定范围
  └─→ 用户要求处理什么？具体路径是哪里？
  └─→ 这个问题属于哪一层？（认知/规则/执行/记忆）

Step 3: 做规划
  └─→ 拆分任务为可验证的子步骤
  └─→ 判断哪些 subagent 角色需要参与
  └─→ 预估风险和边界情况

Step 4: 执行 + 验证
  └─→ 按规划执行
  └─→ 每完成一个子步骤，验证是否符合完成标准
  └─→ 发现偏差时，立即记录到 corrections.md

Step 5: 写回 + 沉淀
  └─→ 把结果写回具体文件
  └─→ 更新 `00-行动中枢/Meta/日志/变更日志.md`
  └─→ 判断是否需要新增 learned-rules
  └─→ 向用户汇报时，明确说明"做了什么"和"下次会自动生效什么"
```

---

## 五、系统进化方向

当前版本：V1.1（已引入 memory 层）

**下一步进化目标**：
1. V1.2：rules 和 hooks 自动触发（减少人工检查）
2. V1.3：subagent 角色自动化分工（plan/validate/archive 自动路由）
3. V2.0：引入 token efficiency 杠杆（大小模型组合，低成本高并发）

**当前最大瓶颈**：
- learned-rules 的生效依赖人工读取，没有自动拦截机制
- 重复检查仍需手动执行
- 缺乏"每周复盘"的自动化提醒

---

*最后更新：2026-05-08*
*更新规则：只有原则边界、完成标准、思考顺序发生变更时才更新。*
